---
"ox": minor
---

Added per-wordlist deep exports under `ox/mnemonic/*` and split `EnsNormalize` into its own module so consumers can opt out of the unused BIP39 wordlists and the `@adraffy/ens-normalize` payload.
